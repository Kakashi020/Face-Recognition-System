# Face-Recognition-System



You are suppose to make a folder and put this files inside it 
and make one folder inside the folder to store the picture to train it by your self or just run the "run_recognition.py" 
and your allow permissions then you will get option to add new faces via camera itself.

